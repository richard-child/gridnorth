!!!
%html
  %head
    %title= current_page.data.title || "Travel startup? Stop fighting UI templates and learn how design can solve real problems &mdash; Grid North Design"
    %meta{charset: "utf-8"}/
    %meta{content: "IE=edge,chrome=1", "http-equiv" => "X-UA-Compatible"}/
    %meta{content: "Grid North Design provides mobile design services for travel startups.", name: "description"}/
    %meta{content: "mobile, design, richard, child, grid, north, gridnorthdesign, travel, startup", name: "keywords"}/
    %meta{content: "width=device-width, initial-scale=1.0, user-scalable=no", name: "viewport"}/
    %link{rel: "shortcut icon", href: "/images/favicon.ico"}

    = stylesheet_link_tag "main"
    = javascript_include_tag "main"
    = partial("/partials/javascripts/head")

  %body{class: page_classes}
    = partial("/partials/header")
    = yield
    = partial("/partials/footer")
